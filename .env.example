# Copia a .env.local para desarrollo con `vercel env pull` o a mano.
# Se generan solas al crear el KV en Vercel Dashboard > Storage > Create > KV
# y conectar el proyecto, o con: vercel link + vercel env pull
KV_REST_API_URL=https://xxx.upstash.io
KV_REST_API_TOKEN=xxx
